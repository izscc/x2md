# X2MD v1.1.13

## 修复

- 修复点击 X/Twitter 书签按钮保存 Article 时，整条推文卡片的图片被集中插到 Markdown 开头的问题。
- Article 页面提取重新限制在长文正文容器内，避免把 status 预览图、母贴图片和操作区误认为正文。
- 服务端生成 Article Markdown 时不再把 `images` 兜底列表前置；已在正文中的图片不重复，未内联的兜底图片只追加在正文末尾。

## 验证

- Python 单元测试：10/10 通过。
- Node 单元测试：48/48 通过。
- 扩展核心脚本语法检查通过。
