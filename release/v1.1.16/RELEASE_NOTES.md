# X2MD v1.1.16

## 新增

- 新增“开机自动运行”选项：可在浏览器设置页开启或关闭。
- 托盘菜单新增“开机自动运行”开关，方便不打开设置页时直接调整。

## 修复

- App 启动时会先检查 `127.0.0.1:9527` 是否已有 X2MD 服务在线，避免重复启动导致端口抢占。
- 开启新版自启时会自动移除旧版 `com.x2md.server` LaunchAgent，避免旧路径服务和 App 内置服务互相干扰。

## 验证

- Python 单元测试：11/11 通过。
- 扩展核心脚本语法检查通过。
- 本地 `/ping` 健康检查通过。
